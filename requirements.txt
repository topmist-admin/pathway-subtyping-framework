# Frozen dependency list — pinned to the environment verified against the test
# suite on 2026-07-09 (299 passed / 1 skipped in the validation + benchmark +
# pipeline + integration scope; the 2 new modules test_metrics_ari_guard.py and
# test_confound_gate.py = 19/19 pass).
#
# These are EXACT pins for reproducibility. For a normal, range-based install use
#   pip install pathway-subtyping           # resolves from pyproject.toml
# or, to reproduce the verified environment exactly:
#   pip install -r requirements.txt
#
# Mirrors pyproject.toml [project.dependencies] (+ the dev test runner).

# --- Core runtime dependencies ---
numpy==2.4.4
pandas==3.0.2            # tested on 3.x; pyproject cap lifted to <4.0.0 (was <3.0.0) — see CHANGELOG 2026-07-09
scikit-learn==1.9.0
scipy==1.17.1
pyyaml==6.0.3
click==8.4.1
matplotlib==3.11.0
seaborn==0.13.2
jinja2==3.1.6
tqdm==4.68.2
requests==2.32.5        # core dep in pyproject; previously omitted from this file

# --- Test runner (pyproject [project.optional-dependencies].dev) ---
# Needed to run the test suite. Full dev extras (pytest-cov, black, isort,
# flake8, mypy, pre-commit) install via: pip install pathway-subtyping[dev]
pytest==9.1.1

# --- Optional extras (install via: pip install pathway-subtyping[extra]) ---
# [vcf]       — pysam>=0.21.0,<1.0.0
# [viz]       — plotly>=5.15.0,<6.0.0, umap-learn>=0.5.0,<1.0.0, kaleido>=0.2.0,<1.0.0
# [sc]        — anndata>=0.9.0,<1.0.0
# [graph]     — networkx>=3.0,<4.0, py4cytoscape>=1.0.0 (+ Cytoscape desktop app)
# [notebooks] — jupyter, ipykernel, nbformat
# [gnn/harmonize/perturb/embed/genesets] — torch>=2.0.0 (+ transformers for perturb)
# [docs]      — sphinx>=6.0.0, sphinx-rtd-theme>=1.2.0, myst-parser>=1.0.0
