# Gate Ablation Study — results (R3.10)

> ⚠️ **SUPERSEDED WORDING — read `../README.md` "READ FIRST" and
> `ablation_honest.json` before quoting anything here.**
>
> This file was machine-generated by an earlier version of
> `scripts/gate_ablation_study.py` whose summary prose was misleading in two ways
> that a hostile-review pass confirmed against the raw CSV:
>
> 1. **The FPR 0.00 cells are computed against n=30 and count abstentions as
>    correct rejections.** The gate returns `not-testable` on 28 of the 30
>    negative controls (93%); the testable-negative denominator is **2**, giving a
>    Wilson interval of [0.00, 0.66]. Do not quote "FPR 0.000" from the table below.
> 2. **"while retaining true positives" is wrong.** TPR moves 1.000 → 0.967 — one
>    `discrete_k3` replicate is lost. Exact McNemar p=1.0, so the cost is not
>    distinguishable from zero, but it is not zero either. Report both numbers.
>
> The **script has been corrected**, so re-running it regenerates this file with
> honest prose and three-way CERTIFY/REJECT/ABSTAIN labels. That re-run takes
> ~77 min and has not been performed; this banner stands in until it is. The
> numeric table itself is accurate — only the framing was wrong.

n=60 samples, p=50 pathways, 15 reps/condition, n_ref=120, n_bootstrap=40. Elapsed 4623.9s.

**TPR** = fraction of genuine-subtype datasets certified (higher is better). **FPR** = fraction of no-structure datasets (single Gaussian + continuum) falsely certified as subtypes (lower is better).

| Gate subset | TPR | FPR | continuum_1d cert rate |
|---|---|---|---|
| `stability_only` | 1.00 | 0.37 | 0.73 |
| `discreteness_only` | 0.97 | 0.00 | 0.00 |
| `stability+discreteness` | 0.97 | 0.00 | 0.00 |

**Reading:** `stability_only` (the pre-v0.8.0 gate set) false-certifies the 1-D continuum — the erratum finding. `stability+discreteness` (v0.8.0) drives the continuum false-positive rate down while retaining true positives, which is the quantitative answer to R3.10: removing the discreteness gate reintroduces continuum false positives.