# PSF public/private split — blocked keywords (committed, public-safe)
#
# One pattern per line. Extended regex (grep -E). Case-insensitive.
# Lines starting with # and blank lines are ignored.
#
# Keywords here are public program names and acronyms whose *presence*
# in a public commit would reveal the private fork's focus. Blocking
# them on the public branch is the point — it's not that the words
# themselves are secret, it's that the public PSF edition should never
# mention them.
#
# Names of individuals and private partner companies go in the
# GITIGNORED companion file: blocked-keywords.local.txt
# Do NOT add names or partner companies here.

# --- DARPA / program acronyms ---------------------------------------------
\bDARPA\b
\bBPU\b
\bBPUs\b
O-Circuit
OCircuit
\bO_Circuit\b

# --- Federal funding agencies / programs ----------------------------------
# These reveal the private fork's grant-pipeline strategy. The public PSF
# edition is a scientific tool and should never name the agencies it is
# being positioned for. (Generic science words like "grant" are NOT listed.)
ARPA-?H
\bNHGRI\b
\bNIDCR\b
\bCDMRP\b
\bSBIR\b
\bSTTR\b
Bridge2AI
\bBTO\b
\bARPA-H CIRCLE\b

# --- Research-compliance / controlled-data-access PURSUIT status ---------
# The public PSF edition operates on already-public de-identified data and has no
# reason to mention a Federalwide Assurance — that vocabulary only appears when the
# private fork is pursuing controlled-access data. So FWA is blocked here.
# NOT listed (normal science, legitimately public): public repository names
# (CommonMind, dbGaP, GTEx, recount3, PsychENCODE, SFARI, NDA) and generic
# compliance resources (OHRP/HHS, CITI, NIH data-sharing) as cited in the
# already-public contributor kit.
\bFWA\b

# --- O-Circuit domain vocabulary that only appears in the BPU context ----
# (generic "organoid" is fine in public — NOT listed)
biological processing unit
biological processing units

# --- Compound phrases that only make sense in the private context --------
CONFIDENTIAL — TOPMIST LLC PROPRIETARY
Topmist LLC Proprietary
molecular-informed decoder
molecular informed decoder
MSV injection
MSVPlus
7th MSV dimension

# --- Private repo / workspace identifiers --------------------------------
topmistbpu
pathway-subtyping-framework.*private
